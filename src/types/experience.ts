export type Experience = {
  id: string
  role: string
  period: string
  company: string
  description: string
}

export type ExperienceDictionary = {
  role: string
  title: string
  heading: string
  current: string
}
